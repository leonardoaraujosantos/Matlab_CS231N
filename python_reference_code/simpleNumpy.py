import numpy as np
def createNumpyMatrix(rows, cols):
    # Use numpy to create a random matrix
    return np.random.random((rows,cols))

def getMatrixShape(numpyMatrix):
    # Use numpy to create a random matrix
    return numpyMatrix.shape
